from downloader import to_interval_


def load_candles(symbol, time_frame, start_time, end_time):
    # loads the csv files
    # filters the csv files to get only the rows which fall into start_time and end_time
    # create a candle for each row in the filtered data
    pass
